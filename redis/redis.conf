# DevopShift Store — opinionated Redis config for the cache sidecar.
# Two settings the lab discusses; everything else is upstream defaults.

# Cap memory so the cache can never starve the host.
maxmemory 256mb

# Persist writes to an append-only file so cache state survives a Redis
# restart. Same volume idea as Topic 5, applied to Redis itself.
appendonly yes
